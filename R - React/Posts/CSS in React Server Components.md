TAGS: #react 
TITLE: CSS in React Server Components
URL: https://www.joshwcomeau.com/react/css-in-rsc/
AUTHOR: Josh Comeau

---

- React Server Components provides a more intuitive solution to this problem. With RSC, we can do database calls and other server-exclusive work right in our React components:

- we need to make sure that we understand Server Side Rendering (SSR).

- When this HTML document loads on the user’s device, React will re-render all of the same components,

- Instead of generating new HTML elements, however, it “adopts” the HTML elements that were generated by the server.* This is known as hydration.

- React needs to perform the exact same work in order to reconstruct the component tree

- The real innovation is that Server Components and Client Components can co-exist in the same application!

- Essentially, every styled component is a regular React component, but with an extra lil’ side effect: they also render their styles to a <style> tag.

- The fundamental incompatibility is that styled-components are designed to run in-browser, whereas Server Components never touch the browser.

